spring:
  application:
    name: ecycle-gateway
  profiles:
    active: dev
  main:
    web-application-type: reactive
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml
        server-addr: localhost:8848