spring:
  application:
    name: ecycle-auth
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml
        server-addr: 127.0.0.1:8848